import LoggedInContext, {
  Provider,
} from "../components/contexts/LoggedInContext";

describe("check if useState will update login object", () => {
  const state = {
    loggedIn: false,
    token: null,
  };
});
// todo sprawdzić, czy wyzerowanie tokena wyloguje usera też
